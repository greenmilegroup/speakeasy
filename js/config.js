/* =========================================================================
   SPEAKEASY — site configuration
   Paste your Supabase project values here when you have them. Until then the
   site reads events from assets/data/events.json and everything still works.
   NEVER put the service-role key in this file — it is public.
   ========================================================================= */
export const SUPABASE_URL      = '';   // e.g. 'https://abcdefgh.supabase.co'
export const SUPABASE_ANON_KEY = '';   // the public "anon" key — safe to ship
export const EVENTBRITE_URL    = 'https://www.eventbrite.ca/o/speakeasy-tapas-lounge-57744410063';
export const TEL               = '+16132416221';
